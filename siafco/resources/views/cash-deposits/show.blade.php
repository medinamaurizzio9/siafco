<x-layouts.app title="Detalle de rendición de caja">
    <div class="mb-4 flex items-center justify-between gap-3"><div><h2 class="text-3xl font-black text-[#0b1f3a]">Detalle de rendición</h2><p class="text-slate-600">Información original del depósito registrado por Caja.</p></div><a class="btn-secondary" href="{{ route('cash-deposits.admin.index') }}">Volver</a></div>
    <section class="section-card"><dl class="grid gap-4 md:grid-cols-2"><div><dt class="text-xs font-black uppercase text-slate-500">Cajero</dt><dd>{{ $deposit->registrar?->name }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Monto</dt><dd>{{ $deposit->currency }} {{ number_format((float)$deposit->amount, 2) }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Fecha del depósito</dt><dd>{{ $deposit->deposited_at->format('d/m/Y H:i') }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">N.º de transacción</dt><dd>{{ $deposit->transaction_number }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Estado</dt><dd>{{ $deposit->status === 'confirmed' ? 'Confirmado' : ($deposit->status === 'rejected' ? 'Rechazado' : 'En revisión') }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Revisado por</dt><dd>{{ $deposit->reviewer?->name ?? 'Pendiente' }}</dd></div>@if($deposit->observations)<div><dt class="text-xs font-black uppercase text-slate-500">Observación</dt><dd>{{ $deposit->observations }}</dd></div>@endif @if($deposit->rejection_reason)<div><dt class="text-xs font-black uppercase text-red-700">Motivo de rechazo</dt><dd>{{ $deposit->rejection_reason }}</dd></div>@endif</dl>@if($deposit->voucher_path)<a class="btn-secondary mt-5" target="_blank" href="{{ route('cash-deposits.admin.voucher', $deposit) }}">Ver comprobante</a>@endif</section>
</x-layouts.app>
