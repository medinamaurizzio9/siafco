<x-investment-crm.advisor.layout title="Seguimientos" :advisor="$advisor"><h1 class="crm-title">Mis seguimientos</h1><p class="crm-muted mt-1">Organiza tus conversaciones pendientes.</p>@foreach(['HOY'=>$today,'VENCIDOS'=>$overdue,'PRÓXIMOS'=>$upcoming] as $label=>$items)<section class="mt-6"><div class="flex items-center gap-2"><h2 class="crm-section-title">{{ $label }}</h2><span class="crm-status">{{ $items->count() }}</span></div><div class="mt-3 grid gap-3 md:grid-cols-2">@forelse($items as $p)<a class="crm-card block" href="{{ route('investment-crm.advisor.prospects.show',$p) }}"><strong class="text-lg">{{ \App\Support\InvestmentCrmDate::format($p->next_follow_up_at,'H:i') }} · {{ $p->full_name }}</strong><p class="crm-muted mt-1">{{ $p->prospect_number }} · {{ ucfirst($p->preferred_contact_method) }}</p><span class="mt-3 inline-block font-bold text-blue-700">Ver →</span></a>@empty<p class="crm-muted text-sm">Sin seguimientos.</p>@endforelse</div></section>@endforeach</x-investment-crm.advisor.layout>
