<x-investment-crm.advisor.layout title="Mis prospectos" :advisor="$advisor"><h1 class="crm-title">Mis prospectos</h1><p class="crm-muted mt-1">Personas asignadas a tu gestión comercial.</p><div class="mt-5 grid gap-3 md:grid-cols-2">@forelse($prospects as $p)<article class="crm-card"><div class="flex justify-between gap-3"><strong>{{ $p->prospect_number }}</strong><span class="crm-status crm-status--{{ $p->status }}">{{ \App\Support\InvestmentProspectStatus::label($p->status) }}</span></div><h2 class="mt-3 text-lg font-bold">{{ $p->full_name }}</h2><p class="crm-muted">{{ $p->phone }} · {{ $p->requested_shares }} acciones</p><a class="crm-button crm-button--secondary mt-4 w-full" href="{{ route('investment-crm.advisor.prospects.show',$p) }}">Ver prospecto</a></article>@empty<div class="crm-card md:col-span-2"><p>No tienes prospectos asignados.</p></div>@endforelse</div><div class="mt-4">{{ $prospects->links() }}</div></x-investment-crm.advisor.layout>
